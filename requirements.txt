# Core scraper
playwright>=1.40.0

# HTTP (SOAP enumeration + queue server client)
httpx>=0.27.0

# Central queue server (optional — only needed on the queue server machine)
fastapi>=0.111.0
uvicorn>=0.30.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0
